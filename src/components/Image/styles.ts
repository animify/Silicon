import { CssFelaStyle } from 'react-fela';
import { Theme } from '../../theme/types';
import { ImageProps } from './Image';

export const imageStyles: CssFelaStyle<Theme, ImageProps<any>> = ({ theme, ...props }) => ({});
