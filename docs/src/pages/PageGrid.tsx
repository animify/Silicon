import React from 'react';
import PageHeader from '../PageHeader';

export default function PageGrid() {
    return (
        <>
            <PageHeader title="Grid" description="The Grid component is used for." importName="Grid" />

            <section>Grid</section>
        </>
    );
}
